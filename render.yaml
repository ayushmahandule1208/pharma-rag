# Render Blueprint for PharmaRAG
services:
  - type: web
    name: pharma-rag-api
    env: docker
    plan: free
    healthCheckPath: /api/health
    envVars:
      - key: OPENAI_API_KEY
        sync: false
    disk:
      name: pharma-data
      mountPath: /app/data
      sizeGB: 1

